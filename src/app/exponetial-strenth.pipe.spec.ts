import { ExponetialStrenthPipe } from './exponetial-strenth.pipe';

describe('ExponetialStrenthPipe', () => {
  it('create an instance', () => {
    const pipe = new ExponetialStrenthPipe();
    expect(pipe).toBeTruthy();
  });
});
